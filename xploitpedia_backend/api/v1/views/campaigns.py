#!/usr/bin/python3
from api.v1.views import xiopedia_views
from flask import abort, jsonify, make_response, request

""" Filter limit is 10 - 20 items per request """

# get all items by filter limit

# get one item by id

# get all items linked to given id (stiix items)
    # return format {obj_name : {len : items}}

# get all items created earlier than / later than
    # return format {return_content, item_count + 10}

# get first item

# 

